import { css } from "styled-components";

export const montserratRegularDisplay = css`
  --font-size: 5rem;
  --line-height: 5.5rem;

  font-size: var(--font-size);
  font-weight: 600;
  line-height: var(--line-height);
`;
